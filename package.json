{
  "name": "openfeature-managed-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Managed OpenFeature-style boolean flag gateway with paid production keys",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/flags.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
